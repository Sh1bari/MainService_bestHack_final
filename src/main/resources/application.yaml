server:
  port: 8082
  servlet:
    context-path: /api
spring:
  application:
    name: main-server
  cloud:
    openfeign:
      circuitbreaker:
        enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
      instance:
        preferIpAddress: true